# 📋 WEEK 1-2 IMPLEMENTATION ROADMAP (Exact Code & Steps)\n\n**Status:** Ready to execute  \n**Duration:** 14 days  \n**Outcome:** Complete auth system + search feature + basic booking\n\n---\n\n## 📅 DAILY BREAKDOWN\n\n## **DAY 1: PROJECT SETUP & INFRASTRUCTURE**\n\n### **Frontend Setup (3 hours)**\n\n```bash\n# Step 1: Create Next.js project\nnpx create-next-app@latest trucking-web --typescript --tailwind -y\ncd trucking-web\n\n# Step 2: Install animations & utilities\nnpm install framer-motion axios zod zustand @tanstack/react-query react-hook-form lucide-react\n\n# Step 3: Create folder structure\nmkdir -p src/{components/{common,customer,fleet,driver,admin},hooks,context,lib/api,types,styles}\n\n# Step 4: Copy configuration files from FRONTEND_SETUP_GUIDE.md\n# - tailwind.config.ts\n# - next.config.ts\n# - .env.local\n\n# Step 5: Start dev server\nnpm run dev\n# ✅ Should run on http://localhost:3000\n```\n\n### **Backend Setup (3 hours)**\n\n```bash\n# Step 1: Create project\nmkdir trucking-api\ncd trucking-api\nnpm init -y\n\n# Step 2: Install dependencies (from BACKEND_SETUP_GUIDE.md)\nnpm install express cors dotenv helmet jsonwebtoken bcrypt pg typeorm reflect-metadata zod \\\n  express-rate-limit express-validator multer winston socket.io redis ioredis\n\nnpm install -D nodemon ts-node @types/express @types/node @types/jsonwebtoken @types/bcrypt typescript\n\n# Step 3: Create folder structure\nmkdir -p src/{entities,controllers,services,routes,middleware,validators,utils,types}\n\n# Step 4: Copy core files from BACKEND_SETUP_GUIDE.md\n# - tsconfig.json\n# - .env\n# - src/app.ts\n# - src/server.ts\n# - src/utils/database.ts\n# - src/utils/logger.ts\n\n# Step 5: Update package.json scripts\nnpm set-script dev \"ts-node src/server.ts\"\nnpm set-script build \"tsc\"\n\n# Step 6: Start backend\nnpm run dev\n# ✅ Should run on http://localhost:3001\n```\n\n### **Database Setup (2 hours)**\n\n```bash\n# Using Docker (in trucking-api folder)\ncat > docker-compose.yml << 'EOF'\nversion: '3.8'\nservices:\n  postgres:\n    image: postgres:15-alpine\n    ports:\n      - \"5432:5432\"\n    environment:\n      POSTGRES_USER: trucking_user\n      POSTGRES_PASSWORD: secure_password_123\n      POSTGRES_DB: trucking_db\n    volumes:\n      - postgres_data:/var/lib/postgresql/data\n  redis:\n    image: redis:7-alpine\n    ports:\n      - \"6379:6379\"\nvolumes:\n  postgres_data:\nEOF\n\n# Start services\ndocker-compose up -d\n\n# Verify\ndocker ps  # Should show postgres and redis running\n```\n\n**End of Day 1:** ✅ Three running services (Frontend, Backend, Database)\n\n---\n\n## **DAY 2: SHARED TYPES & DATABASE MODELS**\n\n### **Create Shared Types**\n\n**Backend: src/types/index.ts**\n\n```typescript\n// User Types\nexport enum UserRole {\n  CUSTOMER = 'customer',\n  FLEET_OWNER = 'fleet_owner',\n  DRIVER = 'driver',\n  AGENT = 'agent',\n  ADMIN = 'admin',\n}\n\nexport interface User {\n  id: string\n  email: string\n  phone: string\n  first_name: string\n  last_name?: string\n  role: UserRole\n  avatar_url?: string\n  kyc_verified: boolean\n  is_active: boolean\n  created_at: Date\n  updated_at: Date\n}\n\nexport interface AuthResponse {\n  user: Omit<User, 'password_hash'>\n  token: string\n  refreshToken: string\n}\n\nexport interface ApiResponse<T> {\n  success: boolean\n  message: string\n  data: T | null\n  error?: string\n}\n\n// Truck Types\nexport enum TruckType {\n  HATHI = 'hathi',\n  SHEHZORE = 'shehzore',\n  FRIDGE = 'fridge',\n}\n\nexport interface Truck {\n  id: string\n  fleet_owner_id: string\n  truck_type: TruckType\n  registration_number: string\n  capacity_tons: number\n  base_fare_prs: number\n  per_km_rate_prs: number\n  avg_rating: number\n  total_reviews: number\n  is_insured: boolean\n  has_gps: boolean\n  created_at: Date\n}\n\n// Booking Types\nexport enum BookingStatus {\n  PENDING = 'pending',\n  APPROVED = 'approved',\n  IN_TRANSIT = 'in_transit',\n  COMPLETED = 'completed',\n  CANCELLED = 'cancelled',\n}\n\nexport interface Booking {\n  id: string\n  customer_id: string\n  truck_id: string\n  booking_status: BookingStatus\n  pickup_address: string\n  drop_address: string\n  cargo_type: string\n  weight_tons: number\n  total_amount_prs: number\n  booking_date: Date\n  created_at: Date\n}\n```\n\n### **Create Database Entities**\n\n**Backend: src/entities/User.ts**\n\n```typescript\nimport { Entity, PrimaryGeneratedColumn, Column, CreateDateColumn, UpdateDateColumn, Index } from 'typeorm'\n\nexport enum UserRole {\n  CUSTOMER = 'customer',\n  FLEET_OWNER = 'fleet_owner',\n  DRIVER = 'driver',\n  AGENT = 'agent',\n  ADMIN = 'admin',\n}\n\n@Entity('users')\n@Index('IDX_EMAIL', ['email'], { unique: true })\n@Index('IDX_PHONE', ['phone'], { unique: true })\nexport class User {\n  @PrimaryGeneratedColumn('uuid')\n  id: string\n\n  @Column({ length: 100 })\n  email: string\n\n  @Column({ length: 20 })\n  phone: string\n\n  @Column({ length: 100 })\n  first_name: string\n\n  @Column({ length: 100, nullable: true })\n  last_name: string\n\n  @Column({ length: 255 })\n  password_hash: string\n\n  @Column({\n    type: 'enum',\n    enum: UserRole,\n    default: UserRole.CUSTOMER,\n  })\n  role: UserRole\n\n  @Column({ nullable: true, length: 500 })\n  avatar_url: string\n\n  @Column({ type: 'boolean', default: false })\n  kyc_verified: boolean\n\n  @Column({ type: 'boolean', default: true })\n  is_active: boolean\n\n  @Column({ type: 'text', nullable: true })\n  bio: string\n\n  @CreateDateColumn()\n  created_at: Date\n\n  @UpdateDateColumn()\n  updated_at: Date\n}\n```\n\n**Backend: src/entities/Truck.ts**\n\n```typescript\nimport { Entity, PrimaryGeneratedColumn, Column, CreateDateColumn, ManyToOne, JoinColumn, Index } from 'typeorm'\nimport { User } from './User'\n\nexport enum TruckType {\n  HATHI = 'hathi',\n  SHEHZORE = 'shehzore',\n  FRIDGE = 'fridge',\n}\n\n@Entity('trucks')\n@Index('IDX_REGISTRATION', ['registration_number'], { unique: true })\n@Index('IDX_FLEET_OWNER', ['fleet_owner_id'])\nexport class Truck {\n  @PrimaryGeneratedColumn('uuid')\n  id: string\n\n  @Column('uuid')\n  fleet_owner_id: string\n\n  @ManyToOne(() => User)\n  @JoinColumn({ name: 'fleet_owner_id' })\n  fleet_owner: User\n\n  @Column({ type: 'enum', enum: TruckType })\n  truck_type: TruckType\n\n  @Column({ length: 20 })\n  registration_number: string\n\n  @Column({ type: 'decimal', precision: 5, scale: 1 })\n  capacity_tons: number\n\n  @Column({ type: 'decimal', precision: 10, scale: 2 })\n  base_fare_prs: number\n\n  @Column({ type: 'decimal', precision: 5, scale: 2 })\n  per_km_rate_prs: number\n\n  @Column({ type: 'decimal', precision: 3, scale: 1, default: 0 })\n  avg_rating: number\n\n  @Column({ type: 'int', default: 0 })\n  total_reviews: number\n\n  @Column({ type: 'boolean', default: false })\n  is_insured: boolean\n\n  @Column({ type: 'boolean', default: false })\n  has_gps: boolean\n\n  @Column({ type: 'boolean', default: true })\n  is_available: boolean\n\n  @CreateDateColumn()\n  created_at: Date\n}\n```\n\n**End of Day 2:** ✅ Database schema ready with 2 core entities\n\n---\n\n## **DAY 3: AUTHENTICATION BACKEND - Part 1**\n\n### **Create Auth Validators**\n\n**Backend: src/validators/auth.validator.ts**\n\n```typescript\nimport { z } from 'zod'\n\nexport const authValidator = {\n  sendOTP: z.object({\n    body: z.object({\n      phone: z.string().regex(/^\\+92\\d{10}$/, 'Invalid Pakistani phone number'),\n    }),\n  }),\n\n  verifyOTP: z.object({\n    body: z.object({\n      phone: z.string(),\n      otp: z.string().length(6, 'OTP must be 6 digits'),\n    }),\n  }),\n\n  signup: z.object({\n    body: z.object({\n      email: z.string().email('Invalid email'),\n      phone: z.string().regex(/^\\+92\\d{10}$/, 'Invalid phone'),\n      password: z.string().min(8, 'Password must be 8+ characters'),\n      first_name: z.string().min(2, 'Name required'),\n      role: z.enum(['customer', 'fleet_owner', 'driver']),\n    }),\n  }),\n\n  login: z.object({\n    body: z.object({\n      email: z.string().email('Invalid email'),\n      password: z.string(),\n    }),\n  }),\n}\n```\n\n### **Create Auth Service**\n\n**Backend: src/services/auth.service.ts**\n\n```typescript\nimport bcrypt from 'bcrypt'\nimport jwt from 'jsonwebtoken'\nimport { AppDataSource } from '../utils/database'\nimport { User, UserRole } from '../entities/User'\nimport { logger } from '../utils/logger'\n\ninterface SignupPayload {\n  email: string\n  phone: string\n  password: string\n  first_name: string\n  role: UserRole\n}\n\nexport class AuthService {\n  private userRepository = AppDataSource.getRepository(User)\n\n  async sendOTP(phone: string): Promise<void> {\n    // Generate 6-digit OTP\n    const otp = Math.floor(100000 + Math.random() * 900000).toString()\n    \n    // TODO: Store in Redis with 5-min TTL\n    // const redis = getRedisClient()\n    // await redis.setex(`otp:${phone}`, 300, otp)\n    \n    // TODO: Send via Brevo API\n    // const brevoClient = new BrevoSmsClient()\n    // await brevoClient.sendOTP(phone, otp)\n    \n    // For testing: log OTP\n    logger.info(`📱 OTP for ${phone}: ${otp}`)\n  }\n\n  async verifyOTP(phone: string, otp: string): Promise<boolean> {\n    // TODO: Verify against Redis\n    // const redis = getRedisClient()\n    // const storedOTP = await redis.get(`otp:${phone}`)\n    // if (storedOTP !== otp) throw Error('Invalid OTP')\n    \n    // For testing: accept any 6 digits\n    return /^\\d{6}$/.test(otp)\n  }\n\n  async signup(payload: SignupPayload): Promise<Partial<User>> {\n    // Check if user exists\n    const existingUser = await this.userRepository.findOne({\n      where: [{ email: payload.email }, { phone: payload.phone }],\n    })\n\n    if (existingUser) {\n      throw {\n        statusCode: 400,\n        message: 'User with this email or phone already exists',\n        code: 'USER_EXISTS',\n      }\n    }\n\n    // Hash password with salt rounds = 10\n    const passwordHash = await bcrypt.hash(payload.password, 10)\n\n    // Create user\n    const user = this.userRepository.create({\n      email: payload.email,\n      phone: payload.phone,\n      password_hash: passwordHash,\n      first_name: payload.first_name,\n      role: payload.role,\n    })\n\n    const savedUser = await this.userRepository.save(user)\n    const { password_hash, ...userWithoutPassword } = savedUser\n    return userWithoutPassword\n  }\n\n  async login(email: string, password: string): Promise<{\n    user: Partial<User>\n    token: string\n    refreshToken: string\n  }> {\n    const user = await this.userRepository.findOne({ where: { email } })\n\n    if (!user || !user.is_active) {\n      throw {\n        statusCode: 401,\n        message: 'Invalid email or password',\n        code: 'INVALID_CREDS',\n      }\n    }\n\n    const isPasswordValid = await bcrypt.compare(password, user.password_hash)\n\n    if (!isPasswordValid) {\n      throw {\n        statusCode: 401,\n        message: 'Invalid email or password',\n        code: 'INVALID_CREDS',\n      }\n    }\n\n    const token = jwt.sign(\n      { userId: user.id, email: user.email, role: user.role },\n      process.env.JWT_SECRET!,\n      { expiresIn: process.env.JWT_EXPIRE || '7d' }\n    )\n\n    const refreshToken = jwt.sign(\n      { userId: user.id },\n      process.env.JWT_REFRESH_SECRET!,\n      { expiresIn: process.env.JWT_REFRESH_EXPIRE || '30d' }\n    )\n\n    const { password_hash, ...userWithoutPassword } = user\n    return {\n      user: userWithoutPassword,\n      token,\n      refreshToken,\n    }\n  }\n}\n```\n\n**End of Day 3:** ✅ Complete auth service ready\n\n---\n\n## **DAY 4: AUTHENTICATION BACKEND - Part 2**\n\n### **Create Auth Controller**\n\n**Backend: src/controllers/auth.controller.ts**\n\n```typescript\nimport { Request, Response } from 'express'\nimport { AuthService } from '../services/auth.service'\nimport { logger } from '../utils/logger'\n\nexport class AuthController {\n  private authService = new AuthService()\n\n  async sendOTP(req: Request, res: Response) {\n    try {\n      const { phone } = req.body\n      await this.authService.sendOTP(phone)\n\n      res.json({\n        success: true,\n        message: 'OTP sent successfully',\n        data: { expires_in: 300 }, // 5 minutes\n      })\n    } catch (error: any) {\n      logger.error('Send OTP error:', error)\n      res.status(error.statusCode || 500).json({\n        success: false,\n        message: error.message,\n        error: error.code,\n      })\n    }\n  }\n\n  async signup(req: Request, res: Response) {\n    try {\n      const { email, phone, password, first_name, role } = req.body\n      const user = await this.authService.signup({\n        email,\n        phone,\n        password,\n        first_name,\n        role,\n      })\n\n      res.status(201).json({\n        success: true,\n        message: 'Account created successfully',\n        data: user,\n      })\n    } catch (error: any) {\n      logger.error('Signup error:', error)\n      res.status(error.statusCode || 500).json({\n        success: false,\n        message: error.message,\n        error: error.code,\n      })\n    }\n  }\n\n  async login(req: Request, res: Response) {\n    try {\n      const { email, password } = req.body\n      const result = await this.authService.login(email, password)\n\n      res.json({\n        success: true,\n        message: 'Login successful',\n        data: result,\n      })\n    } catch (error: any) {\n      logger.error('Login error:', error)\n      res.status(error.statusCode || 401).json({\n        success: false,\n        message: error.message,\n        error: error.code,\n      })\n    }\n  }\n}\n```\n\n### **Create Auth Routes**\n\n**Backend: src/routes/auth.routes.ts**\n\n```typescript\nimport { Router, Request, Response } from 'express'\nimport { AuthController } from '../controllers/auth.controller'\nimport { validateRequest } from '../middleware/validate.middleware'\nimport { authValidator } from '../validators/auth.validator'\n\nconst router = Router()\nconst authController = new AuthController()\n\nrouter.post('/send-otp',\n  validateRequest(authValidator.sendOTP),\n  (req, res) => authController.sendOTP(req, res)\n)\n\nrouter.post('/signup',\n  validateRequest(authValidator.signup),\n  (req, res) => authController.signup(req, res)\n)\n\nrouter.post('/login',\n  validateRequest(authValidator.login),\n  (req, res) => authController.login(req, res)\n)\n\nexport default router\n```\n\n### **Create Validation Middleware**\n\n**Backend: src/middleware/validate.middleware.ts**\n\n```typescript\nimport { Request, Response, NextFunction } from 'express'\nimport { AnyZodObject } from 'zod'\n\nexport const validateRequest = (schema: AnyZodObject) => {\n  return async (req: Request, res: Response, next: NextFunction) => {\n    try {\n      await schema.parseAsync({ body: req.body })\n      return next()\n    } catch (error: any) {\n      return res.status(400).json({\n        success: false,\n        message: 'Validation error',\n        errors: error.errors,\n      })\n    }\n  }\n}\n```\n\n**End of Day 4:** ✅ Complete auth backend ready (sendOTP, signup, login)\n\n---\n\n## **DAY 5: AUTHENTICATION FRONTEND**\n\n### **Create Login Page**\n\n**Frontend: src/app/(auth)/login/page.tsx**\n\n```typescript\n'use client'\n\nimport { useState } from 'react'\nimport { useRouter } from 'next/navigation'\nimport axios from 'axios'\nimport Button from '@/components/common/Button'\nimport { motion } from 'framer-motion'\n\nexport default function LoginPage() {\n  const router = useRouter()\n  const [email, setEmail] = useState('')\n  const [password, setPassword] = useState('')\n  const [error, setError] = useState('')\n  const [isLoading, setIsLoading] = useState(false)\n\n  const handleLogin = async (e: React.FormEvent) => {\n    e.preventDefault()\n    setError('')\n    setIsLoading(true)\n\n    try {\n      const response = await axios.post(\n        `${process.env.NEXT_PUBLIC_API_URL}/auth/login`,\n        { email, password }\n      )\n\n      const { token, refreshToken, user } = response.data.data\n\n      // Save to localStorage\n      localStorage.setItem('auth_token', token)\n      localStorage.setItem('refresh_token', refreshToken)\n      localStorage.setItem('user', JSON.stringify(user))\n\n      // Redirect based on role\n      if (user.role === 'customer') {\n        router.push('/customer/dashboard')\n      } else if (user.role === 'fleet_owner') {\n        router.push('/fleet/dashboard')\n      } else if (user.role === 'driver') {\n        router.push('/driver/dashboard')\n      }\n    } catch (err: any) {\n      setError(err.response?.data?.message || 'Login failed')\n    } finally {\n      setIsLoading(false)\n    }\n  }\n\n  return (\n    <div className=\"min-h-screen bg-gradient-to-br from-primary-50 to-white flex items-center justify-center p-4\">\n      <motion.div\n        initial={{ opacity: 0, y: 20 }}\n        animate={{ opacity: 1, y: 0 }}\n        className=\"w-full max-w-md\"\n      >\n        <div className=\"bg-white rounded-xl shadow-lg p-8\">\n          <h1 className=\"text-3xl font-bold text-center mb-2\">Welcome Back</h1>\n          <p className=\"text-center text-gray-600 mb-8\">Sign in to continue</p>\n\n          {error && (\n            <motion.div\n              initial={{ opacity: 0 }}\n              animate={{ opacity: 1 }}\n              className=\"mb-4 p-4 bg-error text-white rounded-lg text-sm\"\n            >\n              {error}\n            </motion.div>\n          )}\n\n          <form onSubmit={handleLogin} className=\"space-y-4\">\n            <div>\n              <label className=\"block text-sm font-medium text-gray-700 mb-2\">\n                Email\n              </label>\n              <input\n                type=\"email\"\n                value={email}\n                onChange={(e) => setEmail(e.target.value)}\n                className=\"w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary-500\"\n                placeholder=\"your@email.com\"\n                required\n              />\n            </div>\n\n            <div>\n              <label className=\"block text-sm font-medium text-gray-700 mb-2\">\n                Password\n              </label>\n              <input\n                type=\"password\"\n                value={password}\n                onChange={(e) => setPassword(e.target.value)}\n                className=\"w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary-500\"\n                placeholder=\"••••••••\"\n                required\n              />\n            </div>\n\n            <Button\n              type=\"submit\"\n              isLoading={isLoading}\n              className=\"w-full\"\n              size=\"lg\"\n            >\n              Sign In\n            </Button>\n          </form>\n\n          <p className=\"text-center text-gray-600 text-sm mt-6\">\n            Don't have an account?{' '}\n            <a href=\"/auth/signup\" className=\"text-primary-500 font-semibold hover:underline\">\n              Sign up\n            </a>\n          </p>\n        </div>\n      </motion.div>\n    </div>\n  )\n}\n```\n\n**End of Day 5:** ✅ Login page working with API integration\n\n---\n\n## **DAY 6: SIGNUP PAGE & AUTH CONTEXT**\n\n### **Create Signup Page**\n\n**Frontend: src/app/(auth)/signup/page.tsx** (Similar to login but with additional fields)\n\n### **Create Auth Context**\n\n**Frontend: src/context/AuthContext.tsx**\n\n```typescript\n'use client'\n\nimport { createContext, useContext, useState, useEffect } from 'react'\nimport { User } from '@/types'\n\ninterface AuthContextType {\n  user: User | null\n  token: string | null\n  isLoading: boolean\n  isAuthenticated: boolean\n  logout: () => void\n}\n\nconst AuthContext = createContext<AuthContextType | undefined>(undefined)\n\nexport function AuthProvider({ children }: { children: React.ReactNode }) {\n  const [user, setUser] = useState<User | null>(null)\n  const [token, setToken] = useState<string | null>(null)\n  const [isLoading, setIsLoading] = useState(true)\n\n  useEffect(() => {\n    // Load user from localStorage on mount\n    const savedToken = localStorage.getItem('auth_token')\n    const savedUser = localStorage.getItem('user')\n\n    if (savedToken && savedUser) {\n      setToken(savedToken)\n      setUser(JSON.parse(savedUser))\n    }\n    setIsLoading(false)\n  }, [])\n\n  const logout = () => {\n    localStorage.removeItem('auth_token')\n    localStorage.removeItem('refresh_token')\n    localStorage.removeItem('user')\n    setUser(null)\n    setToken(null)\n  }\n\n  return (\n    <AuthContext.Provider value={{ user, token, isLoading, isAuthenticated: !!token, logout }}>\n      {children}\n    </AuthContext.Provider>\n  )\n}\n\nexport function useAuth() {\n  const context = useContext(AuthContext)\n  if (context === undefined) {\n    throw new Error('useAuth must be used within AuthProvider')\n  }\n  return context\n}\n```\n\n**End of Day 6:** ✅ Complete authentication system working\n\n---\n\n## **DAY 7: TESTING & INTEGRATION**\n\n### **Test All Auth Endpoints with Postman**\n\n```bash\n# 1. Test Login\nPOST http://localhost:3001/api/auth/login\nBody: {\"email\":\"test@example.com\",\"password\":\"password123\"}\n\n# 2. Test Signup  \nPOST http://localhost:3001/api/auth/signup\nBody: {\n  \"email\":\"newuser@example.com\",\n  \"phone\":\"+923001234567\",\n  \"password\":\"password123\",\n  \"first_name\":\"Ahmed\",\n  \"role\":\"customer\"\n}\n\n# 3. Test Protected Route (with JWT)\nGET http://localhost:3001/api/user/profile\nHeader: Authorization: Bearer {token}\n```\n\n**End of Day 7:** ✅ Full end-to-end auth testing complete\n\n---\n\n## **WEEK 2: SEARCH & DISCOVERY (DAY 8-14)**\n\nSame pattern:\n1. Create database entity (Truck)\n2. Create service logic\n3. Create API endpoints\n4. Create frontend pages\n5. Test end-to-end\n\n**Track your progress on the Todo list as you complete each day!**\n\n---\n\n## 🎯 SUCCESS CRITERIA\n\n**By End of Week 1:**\n- [x] Frontend running on localhost:3000\n- [x] Backend running on localhost:3001\n- [x] Database connected and ready\n- [x] Users can sign up with email/password\n- [x] Users can log in and get JWT token\n- [x] Auth token stored in localStorage\n- [x] Protected routes working\n- [x] All tested with Postman\n\n**By End of Week 2:**\n- [ ] Users can search for trucks\n- [ ] Users can view truck details\n- [ ] Users can place bookings\n- [ ] Users can make payments\n- [ ] Dashboard pages working\n- [ ] Real-time tracking initialized\n\n---\n\n## 🚀 YOU'RE READY TO BUILD!\n\nStart with Day 1 setup today. Follow the exact code patterns.\nEach day completes one feature.\nTest as you go!\n\n