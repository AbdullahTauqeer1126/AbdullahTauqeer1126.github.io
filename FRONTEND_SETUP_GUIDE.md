# 🚀 FRONTEND SETUP GUIDE - WEEK 1 & 2 (Next.js 14 + TypeScript)\n\n**Duration:** 3-4 days to complete  \n**Status:** Ready to build  \n**Technology:** Next.js 14, React 18, TypeScript, Tailwind, Framer Motion\n\n---\n\n## 📋 QUICK START (5 MINUTES)\n\n### **Step 1: Create Next.js Project**\n\n```bash\n# In your terminal\nnpx create-next-app@latest trucking-app --typescript --tailwind\n\n# When prompted:\n# ✅ Would you like to use ESLint? → Yes\n# ✅ Would you like to use Tailwind CSS? → Yes\n# ✅ Would you like your code inside a `src/` directory? → Yes\n# ✅ Would you like to use App Router? → Yes\n# ✅ Would you like to use Turbopack? → Yes (for faster dev)\n\ncd trucking-app\n```\n\n### **Step 2: Install Dependencies**\n\n```bash\nnpm install\n\n# Animation & UI Libraries\nnpm install framer-motion axios zod zustand @tanstack/react-query\n\n# Development dependencies\nnpm install -D @types/node @types/react tailwindcss postcss autoprefixer\n\n# Map & Location\nnpm install @react-google-maps/api\n\n# Form handling\nnpm install react-hook-form\n\n# UI Components\nnpm install lucide-react\n```\n\n### **Step 3: Verify Installation**\n\n```bash\nnpm run dev\n# Open http://localhost:3000 in browser\n```\n\n---\n\n## 📁 PROJECT FOLDER STRUCTURE\n\n```bash\n# Create folders\nmkdir -p src/app/{(auth)/{login,signup,forgot-password},(customer)/{dashboard,search,booking,tracking},(fleet)/{dashboard,trucks,bookings},(driver)/{dashboard,trips,tracking},(admin)/{dashboard,users,analytics},api}\nmkdir -p src/components/{common,customer,fleet-owner,driver,admin}\nmkdir -p src/hooks\nmkdir -p src/context\nmkdir -p src/lib/{api,utils,validators}\nmkdir -p src/styles\nmkdir -p src/types\nmkdir -p public/{images,icons}\n```\n\n### **Resulting Structure:**\n\n```\ntructking-app/\n├── src/\n│   ├── app/\n│   │   ├── (auth)/\n│   │   │   ├── login/\n│   │   │   │   └── page.tsx\n│   │   │   ├── signup/\n│   │   │   │   └── page.tsx\n│   │   │   └── forgot-password/\n│   │   │       └── page.tsx\n│   │   ├── (customer)/\n│   │   │   ├── dashboard/\n│   │   │   │   └── page.tsx\n│   │   │   ├── search/\n│   │   │   │   └── page.tsx\n│   │   │   ├── booking/\n│   │   │   │   └── page.tsx\n│   │   │   └── tracking/\n│   │   │       └── page.tsx\n│   │   ├── (fleet)/\n│   │   │   ├── dashboard/\n│   │   │   │   └── page.tsx\n│   │   │   ├── trucks/\n│   │   │   │   └── page.tsx\n│   │   │   └── bookings/\n│   │   │       └── page.tsx\n│   │   ├── (driver)/\n│   │   │   ├── dashboard/\n│   │   │   │   └── page.tsx\n│   │   │   ├── trips/\n│   │   │   │   └── page.tsx\n│   │   │   └── tracking/\n│   │   │       └── page.tsx\n│   │   ├── (admin)/\n│   │   │   ├── dashboard/\n│   │   │   │   └── page.tsx\n│   │   │   ├── users/\n│   │   │   │   └── page.tsx\n│   │   │   └── analytics/\n│   │   │       └── page.tsx\n│   │   ├── api/\n│   │   │   └── (route handlers if needed)\n│   │   ├── layout.tsx (root layout)\n│   │   └── page.tsx (homepage)\n│   ├── components/\n│   │   ├── common/\n│   │   │   ├── Header.tsx\n│   │   │   ├── Sidebar.tsx\n│   │   │   ├── Footer.tsx\n│   │   │   ├── Button.tsx\n│   │   │   ├── Card.tsx\n│   │   │   └── Input.tsx\n│   │   ├── customer/\n│   │   │   ├── SearchBox.tsx\n│   │   │   ├── TruckCard.tsx\n│   │   │   └── BookingForm.tsx\n│   │   ├── fleet-owner/\n│   │   │   ├── DashboardMetrics.tsx\n│   │   │   └── TruckManagement.tsx\n│   │   ├── driver/\n│   │   │   ├── ActiveTrip.tsx\n│   │   │   └── TripRequests.tsx\n│   │   └── admin/\n│   │       └── UserManagement.tsx\n│   ├── hooks/\n│   │   ├── useAuth.ts\n│   │   ├── useBooking.ts\n│   │   ├── useTrip.ts\n│   │   ├── useLocation.ts\n│   │   └── useApi.ts\n│   ├── context/\n│   │   ├── AuthContext.tsx\n│   │   ├── BookingContext.tsx\n│   │   └── AppContext.tsx\n│   ├── lib/\n│   │   ├── api/\n│   │   │   ├── client.ts (Axios instance)\n│   │   │   ├── endpoints.ts\n│   │   │   └── auth.ts\n│   │   ├── utils/\n│   │   │   ├── formatters.ts\n│   │   │   ├── validators.ts\n│   │   │   └── helpers.ts\n│   │   └── validators/\n│   │       └── schemas.ts (Zod schemas)\n│   ├── types/\n│   │   ├── index.ts (shared types)\n│   │   ├── auth.ts\n│   │   ├── booking.ts\n│   │   └── api.ts\n│   └── styles/\n│       ├── globals.css\n│       └── components.css\n├── public/\n│   ├── images/\n│   └── icons/\n├── next.config.ts\n├── tsconfig.json\n├── tailwind.config.ts\n├── package.json\n└── .env.local\n```\n\n---\n\n## 🔧 CONFIGURATION FILES\n\n### **1. tailwind.config.ts**\n\n```typescript\nimport type { Config } from 'tailwindcss'\n\nconst config: Config = {\n  content: [\n    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',\n    './src/components/**/*.{js,ts,jsx,tsx,mdx}',\n    './src/app/**/*.{js,ts,jsx,tsx,mdx}',\n  ],\n  theme: {\n    extend: {\n      colors: {\n        primary: {\n          50: '#f0f7f0',\n          100: '#e0f0e0',\n          500: '#1B5E20',\n          700: '#154620',\n          900: '#0E3410',\n        },\n        secondary: {\n          500: '#FF6F00',\n          600: '#E65100',\n        },\n        success: '#4CAF50',\n        error: '#F44336',\n        warning: '#FFC107',\n        info: '#2196F3',\n      },\n      fontFamily: {\n        sans: ['var(--font-inter)', 'system-ui', 'sans-serif'],\n      },\n      animation: {\n        'pulse-slow': 'pulse 1.5s ease-in-out infinite',\n        'slide-in': 'slideIn 0.3s ease-out',\n        'fade-in': 'fadeIn 0.2s ease-out',\n      },\n    },\n  },\n  plugins: [],\n}\nexport default config\n```\n\n### **2. .env.local**\n\n```env\n# API\nNEXT_PUBLIC_API_URL=http://localhost:3001/api\nNEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_key_here\n\n# Auth\nNEXT_PUBLIC_AUTH_TOKEN_KEY=auth_token\nNEXT_PUBLIC_REFRESH_TOKEN_KEY=refresh_token\n\n# Features\nNEXT_PUBLIC_ENABLE_ANALYTICS=true\nNEXT_PUBLIC_SENTRY_DSN=\n```\n\n### **3. next.config.ts**\n\n```typescript\nimport type { NextConfig } from 'next'\n\nconst nextConfig: NextConfig = {\n  typescript: {\n    strictNullChecks: true,\n  },\n  images: {\n    remotePatterns: [\n      {\n        protocol: 'https',\n        hostname: '**.s3.amazonaws.com',\n      },\n      {\n        protocol: 'https',\n        hostname: 'cloudinary.com',\n      },\n    ],\n  },\n  headers: async () => [\n    {\n      source: '/api/:path*',\n      headers: [\n        {\n          key: 'Access-Control-Allow-Credentials',\n          value: 'true',\n        },\n      ],\n    },\n  ],\n}\n\nexport default nextConfig\n```\n\n---\n\n## 📝 CORE FILES TO CREATE\n\n### **1. src/types/index.ts** (Shared Types)\n\n```typescript\n// User Types\nexport type UserRole = 'customer' | 'fleet_owner' | 'driver' | 'agent' | 'admin'\n\nexport interface User {\n  id: string\n  email: string\n  phone: string\n  name: string\n  role: UserRole\n  avatar_url?: string\n  kyc_verified: boolean\n  created_at: string\n}\n\nexport interface AuthState {\n  user: User | null\n  token: string | null\n  refreshToken: string | null\n  isLoading: boolean\n  error: string | null\n}\n\n// Booking Types\nexport interface Truck {\n  id: string\n  fleet_owner_id: string\n  truck_type: 'hathi' | 'shehzore' | 'fridge'\n  registration_number: string\n  capacity_tons: number\n  base_fare_prs: number\n  per_km_rate_prs: number\n  avg_rating: number\n  total_reviews: number\n  is_insured: boolean\n  has_gps: boolean\n}\n\nexport interface Booking {\n  id: string\n  customer_id: string\n  truck_id: string\n  booking_status: 'pending' | 'approved' | 'in_transit' | 'completed' | 'cancelled'\n  pickup_address: string\n  drop_address: string\n  cargo_type: string\n  total_amount_prs: number\n  booking_date: string\n}\n\nexport interface Trip {\n  id: string\n  booking_id: string\n  driver_id: string\n  trip_status: 'pending' | 'started' | 'in_transit' | 'completed' | 'cancelled'\n  started_at?: string\n  completed_at?: string\n  distance_km?: number\n}\n\n// API Response Types\nexport interface ApiResponse<T> {\n  success: boolean\n  data: T | null\n  error: string | null\n  message: string\n}\n\nexport interface PaginatedResponse<T> extends ApiResponse<T[]> {\n  total: number\n  page: number\n  limit: number\n  totalPages: number\n}\n```\n\n### **2. src/lib/api/client.ts** (Axios Instance)\n\n```typescript\nimport axios, { AxiosInstance, AxiosError } from 'axios'\n\ninterface ApiError {\n  message: string\n  code: string\n  status: number\n}\n\nclass ApiClient {\n  private client: AxiosInstance\n\n  constructor() {\n    this.client = axios.create({\n      baseURL: process.env.NEXT_PUBLIC_API_URL,\n      timeout: 10000,\n      headers: {\n        'Content-Type': 'application/json',\n      },\n    })\n\n    // Request interceptor - Add auth token\n    this.client.interceptors.request.use((config) => {\n      const token = localStorage.getItem(process.env.NEXT_PUBLIC_AUTH_TOKEN_KEY!)\n      if (token) {\n        config.headers.Authorization = `Bearer ${token}`\n      }\n      return config\n    })\n\n    // Response interceptor - Handle errors\n    this.client.interceptors.response.use(\n      (response) => response,\n      async (error: AxiosError) => {\n        if (error.response?.status === 401) {\n          // Token expired - redirect to login\n          localStorage.clear()\n          window.location.href = '/auth/login'\n        }\n        return Promise.reject(error)\n      }\n    )\n  }\n\n  get<T>(url: string, config?: any) {\n    return this.client.get<T>(url, config)\n  }\n\n  post<T>(url: string, data?: any, config?: any) {\n    return this.client.post<T>(url, data, config)\n  }\n\n  put<T>(url: string, data?: any, config?: any) {\n    return this.client.put<T>(url, data, config)\n  }\n\n  patch<T>(url: string, data?: any, config?: any) {\n    return this.client.patch<T>(url, data, config)\n  }\n\n  delete<T>(url: string, config?: any) {\n    return this.client.delete<T>(url, config)\n  }\n}\n\nexport const apiClient = new ApiClient()\n```\n\n### **3. src/hooks/useAuth.ts** (Authentication Hook)\n\n```typescript\nimport { useCallback, useEffect, useState } from 'react'\nimport { AuthState, User } from '@/types'\nimport { apiClient } from '@/lib/api/client'\n\nexport const useAuth = () => {\n  const [authState, setAuthState] = useState<AuthState>({\n    user: null,\n    token: null,\n    refreshToken: null,\n    isLoading: true,\n    error: null,\n  })\n\n  // Check if user is already logged in on mount\n  useEffect(() => {\n    const token = localStorage.getItem(process.env.NEXT_PUBLIC_AUTH_TOKEN_KEY!)\n    const user = localStorage.getItem('user')\n\n    if (token && user) {\n      setAuthState({\n        user: JSON.parse(user),\n        token,\n        refreshToken: localStorage.getItem(process.env.NEXT_PUBLIC_REFRESH_TOKEN_KEY!),\n        isLoading: false,\n        error: null,\n      })\n    } else {\n      setAuthState((prev) => ({ ...prev, isLoading: false }))\n    }\n  }, [])\n\n  const login = useCallback(async (email: string, password: string) => {\n    setAuthState((prev) => ({ ...prev, isLoading: true, error: null }))\n    try {\n      const response = await apiClient.post('/auth/login', { email, password })\n      const { user, token, refreshToken } = response.data.data\n\n      localStorage.setItem(process.env.NEXT_PUBLIC_AUTH_TOKEN_KEY!, token)\n      localStorage.setItem(process.env.NEXT_PUBLIC_REFRESH_TOKEN_KEY!, refreshToken)\n      localStorage.setItem('user', JSON.stringify(user))\n\n      setAuthState({\n        user,\n        token,\n        refreshToken,\n        isLoading: false,\n        error: null,\n      })\n    } catch (error: any) {\n      setAuthState((prev) => ({\n        ...prev,\n        isLoading: false,\n        error: error.response?.data?.message || 'Login failed',\n      }))\n      throw error\n    }\n  }, [])\n\n  const logout = useCallback(() => {\n    localStorage.clear()\n    setAuthState({\n      user: null,\n      token: null,\n      refreshToken: null,\n      isLoading: false,\n      error: null,\n    })\n  }, [])\n\n  return {\n    ...authState,\n    login,\n    logout,\n    isAuthenticated: !!authState.token,\n  }\n}\n```\n\n### **4. src/components/common/Button.tsx** (Reusable Button)\n\n```typescript\nimport React from 'react'\nimport { motion } from 'framer-motion'\n\ninterface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {\n  variant?: 'primary' | 'secondary' | 'danger' | 'ghost'\n  size?: 'sm' | 'md' | 'lg'\n  isLoading?: boolean\n  children: React.ReactNode\n}\n\nconst Button = React.forwardRef<HTMLButtonElement, ButtonProps>((\n  { variant = 'primary', size = 'md', isLoading = false, children, className, ...props },\n  ref\n) => {\n  const baseStyles = 'font-semibold rounded-lg transition-all duration-200 disabled:opacity-60 disabled:cursor-not-allowed'\n\n  const variantStyles = {\n    primary: 'bg-primary-500 text-white hover:bg-primary-700 active:bg-primary-900',\n    secondary: 'bg-white border-2 border-primary-500 text-primary-500 hover:bg-primary-50',\n    danger: 'bg-error text-white hover:bg-red-700 active:bg-red-900',\n    ghost: 'bg-transparent text-primary-500 hover:bg-primary-50',\n  }\n\n  const sizeStyles = {\n    sm: 'px-3 py-1.5 text-sm',\n    md: 'px-6 py-3 text-base',\n    lg: 'px-8 py-4 text-lg',\n  }\n\n  return (\n    <motion.button\n      ref={ref}\n      whileHover={{ scale: 1.02 }}\n      whileTap={{ scale: 0.98 }}\n      className={`${baseStyles} ${variantStyles[variant]} ${sizeStyles[size]} ${className || ''}`}\n      disabled={isLoading || props.disabled}\n      {...props}\n    >\n      {isLoading ? (\n        <span className=\"flex items-center gap-2\">\n          <span className=\"w-4 h-4 border-2 border-current border-t-transparent rounded-full animate-spin\" />\n          Loading...\n        </span>\n      ) : (\n        children\n      )}\n    </motion.button>\n  )\n})\n\nButton.displayName = 'Button'\nexport default Button\n```\n\n### **5. src/components/common/Card.tsx** (Reusable Card)\n\n```typescript\nimport React from 'react'\nimport { motion } from 'framer-motion'\n\ninterface CardProps extends React.HTMLAttributes<HTMLDivElement> {\n  children: React.ReactNode\n  hoverable?: boolean\n}\n\nconst Card = React.forwardRef<HTMLDivElement, CardProps>((\n  { children, hoverable = false, className, ...props },\n  ref\n) => {\n  return (\n    <motion.div\n      ref={ref}\n      whileHover={hoverable ? { y: -4 } : {}}\n      className={`bg-white rounded-xl p-6 shadow-sm border border-gray-100 transition-shadow duration-200 ${\n        hoverable ? 'hover:shadow-md cursor-pointer' : ''\n      } ${className || ''}`}\n      {...props}\n    >\n      {children}\n    </motion.div>\n  )\n})\n\nCard.displayName = 'Card'\nexport default Card\n```\n\n### **6. src/app/layout.tsx** (Root Layout)\n\n```typescript\nimport type { Metadata } from 'next'\nimport { Inter } from 'next/font/google'\nimport './globals.css'\n\nconst inter = Inter({ subsets: ['latin'] })\n\nexport const metadata: Metadata = {\n  title: 'Trucking App - Book & Track Trucks in Pakistan',\n  description: 'Pakistan\\'s leading truck booking and real-time tracking platform',\n  icons: {\n    icon: '/favicon.ico',\n  },\n}\n\nexport default function RootLayout({\n  children,\n}: {\n  children: React.ReactNode\n}) {\n  return (\n    <html lang=\"en\">\n      <body className={inter.className}>\n        {children}\n      </body>\n    </html>\n  )\n}\n```\n\n### **7. src/app/page.tsx** (Homepage)\n\n```typescript\nimport { motion } from 'framer-motion'\nimport Link from 'next/link'\nimport Button from '@/components/common/Button'\n\nexport default function Home() {\n  return (\n    <div className=\"min-h-screen bg-gradient-to-br from-primary-50 to-white\">\n      {/* Header */}\n      <header className=\"sticky top-0 z-50 bg-white shadow-sm\">\n        <div className=\"max-w-7xl mx-auto px-6 py-4 flex justify-between items-center\">\n          <div className=\"text-2xl font-bold text-primary-500\">TruckHub</div>\n          <nav className=\"flex gap-4\">\n            <Link href=\"/auth/login\">\n              <Button variant=\"ghost\">Login</Button>\n            </Link>\n            <Link href=\"/auth/signup\">\n              <Button>Sign Up</Button>\n            </Link>\n          </nav>\n        </div>\n      </header>\n\n      {/* Hero Section */}\n      <section className=\"max-w-7xl mx-auto px-6 py-20 text-center\">\n        <motion.h1\n          initial={{ opacity: 0, y: 20 }}\n          animate={{ opacity: 1, y: 0 }}\n          transition={{ duration: 0.6 }}\n          className=\"text-5xl font-bold text-gray-900 mb-6\"\n        >\n          Book a Truck in <span className=\"text-primary-500\">2 Minutes</span>\n        </motion.h1>\n        <motion.p\n          initial={{ opacity: 0, y: 20 }}\n          animate={{ opacity: 1, y: 0 }}\n          transition={{ duration: 0.6, delay: 0.2 }}\n          className=\"text-xl text-gray-600 mb-8 max-w-2xl mx-auto\"\n        >\n          Pakistan's most reliable truck booking platform with real-time tracking and guaranteed safety.\n        </motion.p>\n        <motion.div\n          initial={{ opacity: 0, y: 20 }}\n          animate={{ opacity: 1, y: 0 }}\n          transition={{ duration: 0.6, delay: 0.4 }}\n          className=\"flex gap-4 justify-center\"\n        >\n          <Link href=\"/customer/search\">\n            <Button size=\"lg\">Book Now</Button>\n          </Link>\n          <Button variant=\"secondary\" size=\"lg\">Learn More</Button>\n        </motion.div>\n      </section>\n\n      {/* Features */}\n      <section className=\"max-w-7xl mx-auto px-6 py-20\">\n        <h2 className=\"text-3xl font-bold text-center mb-12\">Why Choose TruckHub?</h2>\n        <div className=\"grid md:grid-cols-4 gap-8\">\n          {[\n            { icon: '📍', title: 'Real-Time Tracking', desc: 'Track your shipment live' },\n            { icon: '💰', title: 'Transparent Pricing', desc: 'No hidden charges' },\n            { icon: '✅', title: 'Trusted Drivers', desc: 'Verified professionals' },\n            { icon: '🔒', title: 'Safe & Secure', desc: 'Insured shipments' },\n          ].map((feature, i) => (\n            <motion.div\n              key={i}\n              initial={{ opacity: 0, y: 20 }}\n              whileInView={{ opacity: 1, y: 0 }}\n              transition={{ delay: i * 0.1 }}\n              className=\"text-center\"\n            >\n              <div className=\"text-4xl mb-4\">{feature.icon}</div>\n              <h3 className=\"font-bold text-lg mb-2\">{feature.title}</h3>\n              <p className=\"text-gray-600\">{feature.desc}</p>\n            </motion.div>\n          ))}\n        </div>\n      </section>\n    </div>\n  )\n}\n```\n\n---\n\n## ✅ WEEK 1-2 CHECKLIST\n\n- [x] Project created with Next.js 14\n- [x] All dependencies installed\n- [x] Folder structure created\n- [x] Tailwind CSS configured\n- [x] Base types defined (TypeScript)\n- [x] API client setup with Axios\n- [x] Authentication hook created\n- [x] Reusable components (Button, Card)\n- [x] Homepage created with Framer Motion\n- [ ] **NEXT:** Create Auth pages (Login, Signup)\n- [ ] **NEXT:** Setup Auth context\n- [ ] **NEXT:** Create protected routes\n- [ ] **NEXT:** Connect to Backend API (once ready)\n\n---\n\n## 🧪 TESTING\n\n```bash\n# Run development server\nnpm run dev\n\n# Open in browser\n# http://localhost:3000\n\n# You should see:\n# ✅ TruckHub logo\n# ✅ Hero section with CTA buttons\n# ✅ Features grid (4 cards)\n# ✅ Animations working smoothly\n# ✅ Responsive design on mobile\n```\n\n---\n\n## 🚀 NEXT PHASE (Week 3)\n\nOnce Backend is ready, we'll:\n1. Create Login/Signup pages\n2. Connect auth API\n3. Setup authentication flow\n4. Create customer search page\n\n**Status:** Frontend Foundation Ready ✅\n\n